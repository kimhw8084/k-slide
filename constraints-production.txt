# Production dependency candidate lock for the heavy CPU image.
# These versions are deployment inputs, not a certification result; the heavy
# image and networkless doctor must pass before this file is promoted. OpenCode
# and LibreOffice are pinned by the deployment image/profile, not by pip.
# OpenCode: 1.3.9 (compatibility target; inference is currently blocked)
# LibreOffice: UNSET_UNTIL_HEAVY_PROOF
# Python: 3.11 (container runtime)
Pillow==12.3.0
PyMuPDF==1.28.2
python-pptx==1.0.2
paddlepaddle==3.0.0
paddleocr==3.0.3
