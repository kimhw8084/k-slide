# K-Slide v6 Smoke Tests

1. Put one Korean slide screenshot in `.k-slide-input/` and run `/k-slide`.
2. Confirm run folder exists.
3. Confirm `05_final_report.md` contains English-native reconstruction.
4. If slide has a table, confirm table appears as Markdown table.
5. Confirm `06_verification.md` includes table/cardinality checks.
6. Confirm no `<|tool_call|>call:task` appears.
