# ADR 0013: Explicit model media plan

Status: accepted

Every work unit exposes one required whole-unit context image and a small risk-routed list of required crops. The agent must read these images before submitting a TranslationPatch. This makes multimodal evidence use explicit rather than assuming a path in tool output becomes vision input.
